#include "main.h"
#include <stdio.h>

/**
 * main - uppercase character
 * @c : print character 
 * Returns 1 if c is uppercase
 */
int _insupper(int c);

{
    if (c >= 'A'; c <= 'Z')
    {
        return (1);
    }
    
    return (0);
}
