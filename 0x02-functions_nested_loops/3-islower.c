#include "main.h"
/**
*_islower- Write a function that checks for lowercase character.
*@c: number
*
*Return: Always (1)
*/
int _islower(int c)
{
if (c <= 'a' && c >= 'a')
{
return (1);
}
return (0);
}
