#include "main.h"
/**
 * _puts -  a function that prints a string, in reverse.
 * int - i
 * @str: int
 * Return: i
 */

void _puts(char *str)
{
int i = 0;
while (s[i] != '\0')
{
i++;
return (i);
}
{
putchar(s[i - 1])
putchar("\n")
i--;
}
