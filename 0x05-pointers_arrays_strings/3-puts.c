#include "main.h"

/**
 *_puts - a function that prints a string, followed by a new line, to stdout
 *
 *@str: a function that prints a string, followed by a new line, to stdout
 */
void _puts(char *str)
{
printf("%s", str)
printf("\n")
}
